spring:
  application:
    name: eureka-consumer-hystrix
server:
  port: 10026
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10010/eureka/
  instance:
    prefer-ip-address: true
feign:
  hystrix:
    enabled: true